specVersion: "v1"
serviceId: "my-notes-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-notes-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
